{
  "name": "codeguard",
  "version": "1.0.0",
  "private": true,
  "description": "AI-Augmented Code Similarity & Plagiarism Detection Platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "dev:worker": "npm run dev:worker --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "test": "npm run test --workspace=server",
    "test:all": "npm run test --workspace=server && npm run test --workspace=client",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=client",
    "seed": "npm run seed --workspace=server",
    "benchmark": "npm run benchmark --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
